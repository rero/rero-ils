import { CustomBootstrap4Framework } from './custombootstrap4-framework';

describe('CustomBootstrap4Framework', () => {
  it('should create an instance', () => {
    expect(new CustomBootstrap4Framework()).toBeTruthy();
  });
});
